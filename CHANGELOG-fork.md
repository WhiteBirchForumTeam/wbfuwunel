# wbfuwunel `main` —— 這個 fork 的整合分支

`main` 是這個 fork 實際運行的分支：**上游 `tuwunel` 的 `main`，加上下面列出的每一條功能分支**。
這份檔案只記這個 fork 加的東西，不記上游的變更；上游自己的紀錄看 [`RELEASE.md`](RELEASE.md)。

- **`upstream/main`**：上游 [`matrix-construct/tuwunel`](https://github.com/matrix-construct/tuwunel) 的遠端追蹤 ref，永遠不帶本地改動。
- **`upstream-main`**：上游的本地鏡像分支，只快轉。
- **`main`**：上游 + 所有已合併的功能分支（這條）。改動只透過 PR 進來。

> 📌 **這是「這個 fork 到底跟上游差在哪」的唯一權威清單。** `git log --oneline upstream/main..main`
> 只看得到動了什麼，看不到**為什麼**。每一條合併進 `main` 的 **feat、重大 fix、refactor** 分支都要在下面兩張表各留一列；
> 純文件的分支（`docs/*`）不列，它們的產物就在 `docs/design/`，README 那張表是索引。

## 怎麼維護

- 跟上游同步：`git fetch upstream && git branch -f upstream-main upstream/main`，然後在 `main` 上
  `git merge upstream/main`。🚫 **不 rebase、不 force push、不改已推出去的歷史。**
- 開新功能：先在 `docs/design/` 寫方案 → 維護者同意 → 開分支 → 開 PR（目標 `main`）。
- PR 合併之後才寫這裡，而且只寫 feat、重大 fix、refactor。提案階段的東西留在 `docs/design/`。
- **合併紀錄那張表的「備註」要寫為什麼，不只寫做了什麼。** 三個月後讀的人需要的是決定的理由、
  踩到的坑、行為的變化；「加了 X」從 commit 訊息就看得到。
- ⚠️ **行為有變、或有已知限制，一定要寫進來。** 這裡是使用者與維護者會先看到的地方，藏在設計文件
  深處的警告等於沒寫。
- 測試（Windows）：單元 `cargo test -p <crate> --lib`；release build 與實跑見
  [`docs/design/windows-build.md`](docs/design/windows-build.md)；端到端用真伺服器跑：腳本在 [`tests/e2e/`](tests/e2e/README.md)（PR #24 起進 repo；更早的腳本只在各 PR 說明裡）。

## 共用程式碼的不變式

`src/service/media_refs/` 是**媒體持有者的唯一寫入點**（[media-holders.md](docs/design/media-holders.md) §3.1）：任何東西開始或停止持有一份媒體，
都透過它的管理器記帳 —— `hold` / `release` / `swap`（單一外鍵）、`release_all_of` / `swap_all_of`（一則事件或一份備份的全部）、
`release_range`（purge 的範圍）、`release_room`（刪房）、`set_avatar_ref`（頭像）—— 不自己碰五張表（`mxc_holder`、`holder_mxc`、
`room_mxc`、`mxc_room`、`mxc_managed`）。目前的呼叫點：事件寫入（`timeline/append.rs`）、backfill（`timeline/backfill.rs`）、redact
（`timeline/redact.rs`，備份保留時 `swap` 成 `Backup`）、歷史清除（`timeline/purge.rs`）、刪房（`timeline/pdus.rs`）、
retention worker 丟備份（`retention/mod.rs`）、頭像（`profile/mod.rs` 的設定與清除）。

📌 **新增一個寫 `pduid_pdu` 的地方，就欠這個帳一筆。** 漏掉的那個呼叫點會讓那些事件的媒體讀成
「無人持有」，而刪除是不可逆的方向。加持有者的人要先 `hold_media_list` 拿每 mxc 一鎖、commit 後才放。
📌 **新增一個呼叫 `create_file_metadata` 的地方，先想它建的是不是新媒體。** 只有新媒體該拿到 `mxc_managed` 列；
衍生檔（縮圖）拿到列，等於把既存媒體交給掃描（見 review-followups-2026-09-06.md 第 1 條）。

## 功能分支

| 分支 | 做什麼 | 關鍵檔案 |
|---|---|---|
| `feat/system-user` | **伺服器帳號從 `@conduit` 改名 `@system`，顯示名稱 `[SYS] <server_name>`；`@system` 已經是一個不是這台 server 建的帳號時拒絕啟動。** 顯示名稱只存 profile，它的 join 事件跟所有人一樣由 `profile::fill_profile_data` 從 profile 填。`admin::create_server_user` 是唯一建立它的地方（先寫 `global` 表的 `server_user` 記號，再建帳號）；`admin::ensure_server_user` 在 `Services::start` 的遷移之後、任何 worker 之前跑（遷移關掉也跑）：不存在就建立、存在但沒有記號就拒絕啟動、顯示名稱不對就更新。 | `src/service/globals/mod.rs`、`src/service/admin/{create,attachments_notice}.rs`、`src/service/services.rs`、`src/api/client/admin/misc/send_server_notice.rs`、`tests/e2e/{e2e14,e2e9,wbf-helpers}.ps1`、`docs/design/server-user.md` |
| `feat/displayname-leaf` | **新帳號顯示名稱的預設後綴是 🍃**（上游是 💕）。只改 `new_user_displayname_suffix` 的預設值；加後綴的地方仍然只有 `users::full_register` 一處，在註冊當下加一次。 | `src/core/config/mod.rs`、`tuwunel-example.toml` |
| `wbf/recent-erasure` | **`Event/Recent` 與 `Subscribe{cg_seq}` 的補窗照 MSC4025 抹除：寄件人以 `erase: true` 停用後才加入的人拿到裁剪過的事件。** `collect_window` 送出前呼叫跟 `/messages` 同一個 `pdu_metadata.bundle_aggregations`，不另抄一份抹除判斷。 | `src/api/client/wbf/recent.rs`、`tests/e2e/e2e8.ps1`、`docs/design/{room-seq-and-recent,wbf-event-push}.md` |
| `misc/window-bytes-and-roadmap` | **一窗除了則數還有 bytes 上限，先於則數生效；`Batch` 帶 `more`。** `wbf_window_max_bytes`（8 MiB，≥ `wbf_data_max_bytes`）；`core::wbf::events::WindowBudget` 是 `Event/Recent`、`Device/Fetch`、兩種 `Subscribe` 補窗共用的唯一規則（bytes 先問、第一則一定收、一旦拒收全拒、`limit: 0` 不算截斷）。`Event/Batch`／`Device/Batch` 的 `more`：停在上限＝`true`、沒有事件了＝`false`；被截斷的補窗第一個 `Push` 帶 `gap: true`。pack 送一個造一個。⚠️ **client 要跟**：翻不翻下一窗看 `more`，不看 `tc < limit`。 | `src/core/wbf/events.rs`、`src/core/config/{mod,check,tests}.rs`、`src/api/client/wbf/{recent,device,subscribe}.rs`、`src/service/streams/{rooms,devices}.rs`、`tests/e2e/{e2e8,e2e12,wbf-helpers}.ps1`、`docs/design/{wbf-pack-pipeline,room-seq-and-recent,wbf-wire-format,wbf-event-push,wbf-to-device,roadmap}.md` |
| `wbf/recent-rooms` | **`Event/Recent` 可以點名房間：一個房 ＋ `before` 就是那個房的歷史；點名訂閱的補窗只含它自己的房。** `Recent` meta 多一個 `rooms`（省略＝每個加入的房；`[]`＝空窗；點名了不在的房→整個請求 `Forbidden`；同一個房點兩次是一個房）。`collect_window` 收一份房間清單當資料（規則在 `resolve_rooms`），在那裡去重；`Subscribe{rooms, cg_seq}` 的補窗改用訂閱自己的房間清單。沒有新 subtype、沒有新索引：翻頁用 `g_seq`（`pduid_pdu` 的原生 key），`r_seq` 給 client 判斷有沒有洞。 | `src/api/client/wbf/{recent,subscribe}.rs`、`src/core/wbf/vectors.rs`（`recent_one_room_history`）、`tests/e2e/{e2e8,e2e11,wbf-helpers}.ps1`、`docs/design/{room-seq-and-recent,wbf-event-push,wbf-pack-pipeline}.md` |
| `wbf/queue-byte-budget` | **發送佇列多一個 byte 預算；單包 data 上限從 16 MiB 降到 2 MiB。** `PackQueue`（`service/streams`）取代所有裸 `mpsc::Sender<Outgoing>`：包數 `wbf_ws_send_queue_len`（32）與 bytes `wbf_ws_send_queue_bytes`（16 MiB）誰先用完誰擋；額度（semaphore permit）跟著 pack 排隊，送出任務寫完 drop 掉才還。close frame 成本 0、比整個預算大的 pack 拒絕而不是等、push 放不下就丟並記 `gap`。`wbf_data_max_bytes` 2 MiB + 4096、`media_chunk_size_max` 2 MiB；啟動檢查 `check_wbf_send_queue_bytes` 拒絕放不下一個完整 pack 的預算。 | `src/service/streams/{mod,subscribers,rooms,devices}.rs`、`src/api/client/wbf/{mod,ws,draft}.rs`、`src/core/config/{mod,check,tests}.rs`、`tests/e2e/{e2e6,e2e7}.ps1`、`docs/design/{wbf-pack-pipeline,wbf-wire-format,chunked-upload-spec,chunked-upload,wbf-to-device}.md` |
| `wbf/upload-lifecycle` | **Seal 真的串流（本地不再整檔進記憶體、送 S3 的 part 合法），`Status` 與 sweeper 不再刪掉已封存媒體的列。** `store_staging_file` 一律串流（`size` 傳 `None`），part 大小取 provider 自己的（`multipart_part_size() -> Option<usize>`：S3 是設定值且不低於 `S3_MIN_PART_SIZE`，其他回 `None` 由呼叫端決定 —— Seal 用 8 MiB、`put_one` 用整包）；`read_part` 填滿 buffer 才交出去，因為一個 item 就是一個 part。`upload_status` 補上 `upload_locks`（它沒中快取時會寫回快取），sweeper 在鎖下重讀進度列再判過期。啟動檢查 `check_s3_part_size` 拒絕低於 S3 下限的設定。 | `src/service/media/upload.rs`、`src/service/storage/provider.rs`、`src/core/config/{mod,check,tests}.rs`、`tests/e2e/e2e6.ps1`（情境 6／7）、`docs/design/review-followups-2026-09-06.md` §2.2／§2.5 |
| `wbf/id-type` | **[BREAKING] pack 標頭 `id` ＝ `[型別 1 byte] ‖ [值 7 byte]`。** `IdType::{None, ClientConversation, EventPosition, Upload}` 是那張表；期望的型別放進既有的准入表多一欄（不開第二張），dispatcher 在 handler 之前擋型別不符、表沒定義的 byte、以及「無會話卻帶值」。server 鑄的 id（上傳、草稿錨）在 `Ack` 裡回**組好的** id，client 直接抄；client 自己挑號時要帶 `0x01`。上傳 id 因此是 56 bit、media id 是 14 個十六進位字元（既有 16 字元的不受影響，🚫 不要驗長度）。向量檔補上 `0x02 Stream` 七個包，並加一條不變量測試：每筆向量的 id 都要解得出型別、`None` 型別必須整個是 0。 | `src/core/wbf/{id_type,mod,vectors}.rs`、`src/api/client/wbf/{mod,draft}.rs`、`src/service/media/upload.rs`、`docs/design/wbf-wire-format.md` §2.2、`docs/design/wbf-vectors.json`、`tests/e2e/wbf-helpers.ps1`（`Wbf-Id`／`Conv`／`Anchor`） |
| `wbf/channels` | **WS 訂閱與推送：一房一個純記憶體 channel，新事件推給訂閱的連線。** 訂閱者是**連線**（`connection_id`，升級時發），不是 user 也不是 device：一個人的每個裝置都要收，而哪條連線處理是 client 的事。`Event/Subscribe` 點名房間（逐一驗成員資格，不是成員的列進 Ack 的 `skipped`）或不點名 = 帳號層（掃 `userroomid_joined`，之後加入的房由 join hook 跟上）；`Unsubscribe` 退出；server 只往 client 送 `Push`。channel 是 DB 成員資格的投影，由 `state_cache` 一組寫入點的兩個 hook（`follow`／`evict`）維持，`ConnectionGuard` 在連線結束時退光。**推送絕不阻塞 append**：`try_send`，佇列滿就丟並記 `gap`，下一個推得進去的 `Push` 帶 `gap: true`，client 用 `Recent` 補。 | `src/service/channels/mod.rs`、`src/api/client/wbf/subscribe.rs`、`src/service/rooms/timeline/append.rs`、`src/service/rooms/state_cache/update.rs` |
| `wbf/pack-pipeline` | **pack 處理管線第一部分：連線即佇列、每 device 4 條 WS、`Recent` 改成 client 拉窗的 `Event/Batch` 串流。** 一條 WS = 接收 loop ＋ 發送 task（有界 `mpsc`，`wbf_ws_send_queue_len` 32），所有出站經佇列、背壓靠佇列滿；handler 契約 `(services, ctx, view, reply) -> Result<SessionChange, Failure>`，`Reply` 是唯一出口（HTTP 上只裝一個 pack），`admission(kind, subtype)` 一張表答「匿名／HTTP 准不准」。每 (user, device) 最多 `wbf_ws_max_connections_per_device`（4）條 WS，名額 RAII `ConnectionSlot` 掛在 `Session` 上，**在 users service 寫 token 之前**經 `admit` 閘門拿。`Event/Recent` 一次一窗（`limit` 320／500、`batch` 10／100），回 `Event/Batch`（`0x14/0x03`）串流 meta `{tc, bc, fs, ls, r}`、u32 前綴事件、`r = 0` 一窗結束、下一窗帶 `before`；HTTP 回 `Unsupported`。新錯碼 `Unsupported`、`TooManyConnections`。 | `src/api/client/wbf/{mod,ws,recent,session}.rs`、`src/service/connections.rs`、`src/service/users/login.rs`（`AdmitSession`）、`src/core/config/mod.rs`、`src/core/wbf/vectors.rs`、`tests/e2e/e2e7.ps1`（情境 5）、`tests/e2e/e2e8.ps1`、`docs/design/wbf-pack-pipeline.md`、`room-seq-and-recent.md` §2、`wbf-wire-format.md` §3.2／§5／§6.1 |
| `wbf/session-login` | **`Session` kind（`0x10`）：`Login`／`Refresh`／`Logout` 走通道；匿名升級 30 秒內要登入；HTTP 與通道共用的登入限速。** `Login` 的 meta 就是 Matrix `/login` 的請求體，用同一個 ruma 型別解、交給同一組 `handle_login`；登入本體抽到 `users::login::{issue_session, refresh_session, end_session}`，HTTP 路由與通道共用。沒有 `Authorization` header 可升級，未登入只回 `Hello`／`Ping`／`Login`／`Refresh`，`wbf_ws_unauthenticated_timeout`（30 秒，從升級起算）到就 Close 1008。Login 換連線的 Session、Refresh 換 token、Logout 回 Ack 後 Close 1000。`IpTokenBuckets`（從 OIDC 抽出）＋ `login_rc_per_second`／`login_rc_burst_count`（1／10，預設開）管 `/login`、`/refresh`、通道 Login／Refresh 同一個 per-IP bucket。 | `src/api/client/wbf/{session,ws,mod}.rs`、`src/service/users/login.rs`、`src/core/utils/rate_limit.rs`、`src/api/client/session/{mod,refresh,logout,password,token}.rs`、`src/service/oauth/mod.rs`、`src/core/config/mod.rs`、`tests/e2e/e2e7.ps1`（情境 4）、`docs/design/wbf-wire-format.md` §6.3 |
| `wbf/auth-and-ws-lifetime` | **wbf 兩個傳輸都查帳號鎖定；WebSocket 每個 message 重驗 session；關機時等連線結束才放 `Services`。** `authenticate` 回 `Session { user, device, token }` 並跑 `locked_check`（MSC3939）；WS 在每個 binary message **解碼之前** `revalidate`（同 user／device、沒到期、沒被鎖），不過就 `Error(Unauthorized)` ＋ Close 1008。新的 `Services.connections`（`JoinSet`）追蹤升級後的 serve task，`Services::stop` 先 `close_and_join`（最多 15 秒，超時 abort）再 interrupt；loop 自己 `select!` 上 `until_shutdown`，關機送 Close 1001；關機中拒絕新升級（503）。 | `src/api/client/wbf/{mod,ws}.rs`、`src/service/connections.rs`、`src/service/services.rs`、`tests/e2e/e2e7.ps1`（情境 3）、`docs/design/wbf-wire-format.md` §6.1 |
| `media/managed-origin` | **`mxc_managed` 只由建新媒體的入口寫。** `create_file_metadata` 多一個呼叫者必填的 `FileOrigin`：`create` 與分塊上傳的 Seal 傳 `NewMedia` 才寫 `mxc_managed`；上傳縮圖與生成縮圖傳 `DerivedOfExisting` 永不寫，縮圖跟原檔同一個 mxc、壽命跟著原檔。DB 讀錯不寫（不受管＝不刪）。同支：`set_avatar_ref` 從反向索引列出頭像現在持有的全部再換，不信呼叫者讀到的舊值；`has_holders`／`forget_media`／`release_room` 的前綴補 `Interfix`；墓碑的「365 天 TTL」改成永久保留。 | `src/service/media/data.rs`（`FileOrigin`）、`src/service/media/{mod,upload,thumbnail}.rs`、`src/service/media_refs/{mod,holder}.rs`、`src/service/profile/mod.rs`、`tests/e2e/e2e10.ps1`（情境 4）、`docs/design/review-followups-2026-09-06.md` |
| `media/attachments` | **媒體持有者集合取代引用計數；附件隨送訊息宣告；移除 `migrate-references`；fork 自報引擎名。** 媒體不記「被引用幾次」，記「被誰持有」：`mxc_holder`（`mxc ‖ kind ‖ id`）一列一個外鍵 —— 事件 `(room, g_seq)`、原文備份 `(room, backup, g_seq)`、本站頭像 `(avatar, localpart)`；加與拿掉都是集合操作，重做是 no-op。反向索引 `holder_mxc` 給 purge 走範圍、`room_mxc`／`mxc_room` 給刪房與媒體刪除當加速；`mxc_managed`（建立時間）只有這個模型上線後上傳的媒體才有，沒列永不自動刪。E2EE 房間 server 讀不到 content，所以 client 在送訊息的請求裡宣告 `attachments`（HTTP header `X-Wbf-Attachments`，或 wbf pack `Event/Send` kind `0x14`／`0x02`），server 驗（本站、存在、上傳者是 sender、沒墓碑）不過整則拒送；明文照舊自己讀，取聯集。收集器：拿掉持有者 → commit 後在每 mxc 一鎖下 seek 前綴為空就刪；週期掃描 `mxc_managed` 清「從沒被指、超過 `media_unreferenced_grace_seconds`（至少 7 天）」的。非 wbf client 在 E2EE 房傳附件 → server user 開 DM 送一次英文警告。`/versions` 自報 `wbfuwunel`。 | `src/service/media_refs/{mod,holder,collect,attachments}.rs`、`src/database/maps.rs`（五張表）、`src/service/media/data.rs`（`mxc_managed`）、`src/api/client/send.rs`、`src/api/client/wbf/send.rs`、`src/service/rooms/timeline/{append,backfill,redact,purge,pdus}.rs`、`src/service/rooms/retention/mod.rs`、`src/service/profile/mod.rs`、`src/service/admin/attachments_notice.rs`、`tests/e2e/`、`docs/design/{media-holders,media-attachments}.md` |
| `event/room-seq-recent` | **每房連續序號 `r_seq`、全域序號 `g_seq`，與 `Event/Recent`。** 每則進 timeline 的 PDU 存起來時 `unsigned` 帶兩個位置（`org.wbftw.wbfuwunel.r_seq`：房間內 1、2、3…，backfill 歷史 0、−1…；`…g_seq`：本站全域 count 的有號形式），append／backfill 同交易寫、與 `roomid_seqbounds` 計數表一起、在房間 insert lock 下；所有讀路徑自動帶。redact 放回、聯邦送出剝掉、既有庫 startup migration 一次編號。`Event/Recent`（kind `0x14`／`0x01`）：client 帶快取的 `cg_seq`，server 對每個加入的 room 開倒序串流以 `g_seq` k 路合併，只回差異；`{returned, latest_g_seq, complete, next}`。 | `src/core/matrix/pdu/seq.rs`、`src/service/rooms/timeline/{seq,append,backfill,redact}.rs`、`src/service/migrations/backfill_room_seq.rs`、`src/api/client/wbf/recent.rs`、`docs/design/room-seq-and-recent.md` |
| `media/chunked-upload-b` | **分塊上傳的 WebSocket 通道、上傳中間層、規格黃金向量。** `GET /_wbf/v1/ws`：升級時驗 Bearer、每個 binary message 一個 pack、依序處理依序回（client 可連發）、同一個 `handle_pack`、`Hello`、idle 超時（`wbf_ws_idle_timeout`）、超大 frame 由 socket 層拒；連線不保存上傳狀態。中間層：`mediaid_upload` 只剩宣告，新 CF `mediaid_upload_progress` 每塊與塊位置同一 txn；service 記憶體 `uploads_hot` 只放進度與驗塊數字，txn 執行後才推進，每上傳一把鎖。`docs/design/wbf-vectors.json` 由實作產生，client 複製一份對著測。 | `src/api/client/wbf/ws.rs`、`src/service/media/{upload,data,mod}.rs`、`src/database/maps.rs`（`mediaid_upload_progress`）、`src/core/wbf/vectors.rs`、`src/core/config/mod.rs`（`wbf_ws_idle_timeout`） |
| `media/chunked-upload-a` | **分塊上傳／下載，A 支（HTTP 一包一請求）。** fork 自己的二進位 pack（32 byte 外框、CRC-32C、零複製解碼、`data_slot` 給 AEAD 原地寫）與 `POST /_wbf/v1/pack`。`Create` 一次宣告（16 byte `EncryptedFileInfo`：明文總長、明文塊、塊數；data 是 client 加密的檔案描述，server 原樣存還）；塊嚴格有序、`seq` 即塊索引、server **不判斷密文長度**只封上限、每塊記一列位置；`upload_id` 即 mxc；串流模式（`0/0` 哨兵、`IS_LAST` 結束）；單檔上限 10 GiB 超過即截斷仍可 seal；下載按塊或明文位置一次整塊交回；sweeper。規格書 [chunked-upload-spec.md](docs/design/chunked-upload-spec.md)。 | `src/core/wbf/{pack,file_info}.rs`、`src/service/media/{upload,range,data}.rs`、`src/api/client/wbf/mod.rs`、`src/database/maps.rs`（`mediaid_upload`、`mxc_chunk`、`mxc_chunked`）、`src/core/config/mod.rs`（`media_chunk_*`、`media_upload_*`、`wbf_*`） |
| `media/gc-mxc-lock` | **每個 mxc 一鎖。** 關掉收集器「讀到 0 → 刪 bytes」與同時 +1 的毫秒級窗口：+1 的人（事件寫入、backfill、新頭像）在 async 層先持該媒體的鎖、再叫同步寫入、`execute()` 後才放；收集器與 rebuild 持鎖下讀計數再刪。事件裡多個 mxc 排序去重後依序取鎖。結果只剩「+1 先落地、收集器跳過」或「刪除先落地、新引用指向墓碑」兩種。 | `src/service/media_refs/{mod,collect,migrate}.rs`（`MutexMap`、`hold_event_media`、`hold_media`）、`src/service/rooms/timeline/{append,backfill}.rs`、`src/service/profile/mod.rs` |
| `media/gc-delete` | **媒體的真正刪除。** 每個 −1 在交易 commit 後把 mxc 交給收集器（`Txn::on_execute` 掛鉤），收集器照「MIN < 計數 ≤ 0 就刪」動手：刪 bytes、寫墓碑、刪計數列同一交易，之後 GET 回 **410 Gone**（墓碑是終局，365 天 TTL）。負數照刪但先 `error!`；遠端媒體不碰；`media_gc_enabled = false` 只印 log。`!admin media migrate-references [--dry-run]` 離線重算（事件＋原文備份＋本地頭像），哨兵消失、本地孤兒刪除、太新的上傳跳過。 | `src/database/txn.rs`（`on_execute`）、`src/service/media_refs/{mod,collect,migrate}.rs`、`src/service/media/{mod,data,thumbnail}.rs`（`collect`、`Tombstone`、410 傳遞）、`src/admin/media/{migrate_references,refcount,delete}.rs`、`src/core/config/mod.rs`（`media_gc_enabled`、`media_gc_migrate_skip_recent_seconds`） |
| `media/refcount-counter` | **精確的媒體引用計數。** `mxc_refcount: mxc → i64`，±1 走 RocksDB merge operator，是一筆純寫入、排進事件或 profile 既有的交易；讀取時才合併，所以 worker 讀到的是數字不是前綴 seek。redact 不歸零：`save_unredacted_events` 保留的原文備份才是持有者，備份被丟掉的地方才 −1。既存媒體第一次被 ±1 變成哨兵（`i64::MIN`），之後所有 ±1 都被吞掉，等 migrate 重算。**這一版仍不刪任何 byte。** | `src/database/engine/merge.rs`、`src/database/txn.rs`（`Txn::merge`、walker 認得 merge 記錄）、`src/database/engine/descriptor.rs`（`MergeKind`）、`src/service/media_refs/`、`src/service/rooms/retention/mod.rs`（`drop_original`）、`src/admin/media/refcount.rs` |
| `media/refcount-index` | **已被上面那條取代，留作紀錄。** 列式索引 `mxc_holder`（鍵 `mxc ‖ 持有者種類 ‖ 持有者 id`），一列一個持有者，前綴 seek 答「還有沒有人用」。取代的理由：移除引用的當下答不出「現在幾個」，只能事後 seek，那不算計數。CF 已標 `DROPPED`。 | `src/service/media_refs/`（介面保留）、`src/core/matrix/media_ref.rs`（已移除） |

## 合併紀錄

| 日期 | 分支（PR） | 合併 | 備註 |
|---|---|---|---|
| 2026-09-02 | `media/refcount-index`（#5） | ✅ | **上半：只建索引，不刪任何東西。** 起因是 redaction 只把事件剝空、完全不碰媒體 bytes，而且沒有任何索引把事件連到媒體，所以媒體比引用它的每一則訊息都活得久（設計文件 [media-refcount.md](docs/design/media-refcount.md)）。新增服務 `media_refs` 與 CF `mxc_holder`，兩種持有者：事件（寫入／backfill／redact／歷史清除／房間清除五處維護）與個人頭像（設定頭像與停用帳號兩處）。房間頭像是 state 事件，本來就走事件那條。新增 `!admin media list-references`，沒有它「索引是對的」就是不可否證的宣稱。審查抓到 backfill 的 `raw_put` 繞過索引，同支修掉（`c568e77c0`）。真伺服器端到端驗過五個檢查點（多重引用、頭像、redact、刪房間）；變異測試 8/8。**已知限制**：索引只認上線之後的事，舊事件與頭像顯示為無人引用，所以重建工具必須跟刪除一起來。 |
| 2026-09-02 | `media/refcount-counter`（#7） | ✅ | **精確計數取代列式索引，仍不刪任何 byte。** 設計文件 [media-gc.md](docs/design/media-gc.md)：第一版（候選表＋寬限期）被維護者推翻（「redact 當下答不出計數，只能事後 seek，那不算計數」），定案是哨兵懶惰植入零掃描、migrate 離線、上傳不是引用、沒有寬限期。引擎層加 counter merge operator（`engine/merge.rs`，operand 9 bytes：tag + i64，`Init`/`Add`/`Set`；只提供 full merge，partial 一律拒絕）、`Txn::merge`、descriptor 的 `MergeKind`；`mxc_holder` 標 `DROPPED`，新 `mxc_refcount`（Counter）與 `mxc_tombstone`（TTL 365 天，尚未使用）。**⚠️ 三條行為都要知道**：(1) **redact 不歸零** —— `save_unredacted_events`（預設開）保留的原文備份才是持有者，備份被丟掉的地方就是 −1 的地方（retention worker reap、`purge_history` 丟備份，都從備份內容讀 mxc 再扣，同一交易；備份關閉時 redact 當下扣），所以媒體活得跟「管理員還看得到那則訊息」一樣久；(2) **既存媒體不重算也不會被自動清** —— 第一次被 ±1 變成哨兵 `i64::MIN`，`!admin media refcount` 印「no reference count」或「sentinel」，兩者都不是 0；(3) **上傳不是引用** —— `create_file_metadata` 只把列開在 0。**e2e 抓到的 bug（單元＋型別＋build 全綠都沒抓到）**：`Txn::execute` 走過 WriteBatch 的原始表示通知 watcher，`next_record` 只認 put/delete，遇到 merge 記錄（0x2/0x6）回 `None` → `Keys::next` panic → 上傳、換頭像全 500。修法：`Tag` 加 `Merge`/`CfMerge`（版面同 Value），並把原本拿 0x2 當「不認得」的測試改成 0x3。📎 教訓：**加一種寫入原語，要掃所有「解析 WriteBatch 表示」的地方。** 審查（rumia、cirno、salvia 三份 APPROVE）後補：DB 級 round-trip 測試（真 RocksDB 透過 descriptor 掛點註冊 operator，flush＋compact 後再驗）；`drop_original` 先讀備份再另一交易刪的讀後刪競態，用 `retention::Service` 一把 `tokio::sync::Mutex<()>` 從讀到 execute 全程持有關掉（一把全域鎖，因為兩個呼叫端都低頻）；負數計數**定案照刪**（規則就是 MIN < 計數 ≤ 0 就刪），但它是配平 bug 的訊號，worker 刪前要 `error!`。**尚缺的測試**：「備份存在時 purge 只釋放一次」要 Services 才跑得動，目前只有 e2e 涵蓋。`list-references` 改名 `refcount`。 |
| 2026-09-03 | `media/gc-delete`（#10） | ✅ | **照計數動手的那一半。** 設計文件 [media-gc.md](docs/design/media-gc.md) §3.1、§5、§6 同步改成「已實作」。**`Txn::on_execute`**：交易寫入落地、通知 watcher 之後才跑登記的閉包，空 batch 與被丟掉的交易不跑 —— 這是「−1 之後才通知收集器」的保證，七個呼叫點零改動（真 DB 單元測試）。**收集器**：`media_refs` 服務的 worker 收 `mpsc` channel，讀計數，MIN < 計數 ≤ 0 就 `media.collect()`；負數在規則內、照刪，但先 `error!`（配平 bug 的訊號）；遠端媒體留給既有 TTL；`media_gc_enabled = false` 只印「would have deleted」。**墓碑**：`Cbor(Tombstone { deleted_at_secs, reason })`，reason 是 `GarbageCollected`／`Migrated`／`AdminDeleted`，與刪計數列同一交易；擋點在 `Data::search_file_metadata` 開頭回 `Error::Request(NotFound, msg, 410)`，`get`、`get_stored`、`get_or_fetch`、`get_thumbnail` 與縮圖遠端回退都用 `is_gone()` 把 410 原樣傳出。**`migrate-references`**：記憶體重算 `pduid_pdu` ＋ **`retained_pdus_raw()`（原文備份才是持有者，漏了會把已 redact 但備份還在的媒體全當孤兒）** ＋ 本地頭像；非 dry-run 時 `clear()` 後 `Set(計數)` 寫回（沒引用寫 0 不留空，每 1000 列一交易），本地且 ≤ 0 的孤兒刪除，mtime 在 `media_gc_migrate_skip_recent_seconds` 內或讀不到的跳過；worker 期間以 `CollectorPause` Drop guard 暫停（panic 也復位）。**e2e 抓到兩件、單元＋型別＋build 都抓不到**：(1) `get_or_fetch` 用 `if let Ok` 吞掉 GONE 再回「not found」，內容 GET 變 404；(2) 強制殺程序丟掉剛寫的墓碑 —— 引擎 `manual_wal_flush = true`，每筆寫入後才手動 flush，別處持 cork 時那次 flush 被略過，程序內的 WAL 緩衝隨 kill 消失；所有寫入都有這窗口、正常關機會 flush，但 bytes 已刪的紀錄不該是被丟掉的那一半，所以 `collect()` 後多付一次 `engine.flush()`，測試腳本改用 `--execute "server shutdown"` 正常關機。真伺服器 e2e 四情境：計數歸零立刻 410（內容與縮圖）、事後再引用同一 mxc 仍 410、從未引用的上傳留著直到 migrate、保留原文時 reap 後才刪、gc 關閉不刪、舊庫 dry-run 掃到 2 份原文備份只刪真孤兒。三份審查（rumia、cirno、salvia）APPROVE，後補：Drop guard、admin 輸出加「離線維護」警語、`overwrite_counts` 的 O(n×m) 換 `HashSet`。**留給之後**：收集器「讀計數 → 刪 bytes」與同時 +1 的毫秒級窗口（每個 mxc 一鎖，等維護者同意）；purge-releases-once 的 Services 級測試。 |
| 2026-09-03 | `media/gc-mxc-lock`（#12） | ✅ | **關掉 §3.3 那個毫秒級窗口（維護者同意後做）。** 窗口是：收集器讀到 0、有人 +1 落地、收集器刪 bytes，結果是一則活訊息指向消失的檔案。做法是 `media_refs` 服務一張既有工具 `MutexMap<String, ()>`，每個 mxc 一把 in-process 鎖。**+1 那邊**在 **async 呼叫端**先持鎖（`hold_event_media` 對事件內的 mxc 排序去重後依序取，`hold_media` 給新頭像），再叫同步的 `append_pdu_json`／`prepend_backfill_pdu`／`set_avatar_ref`，`execute()` 後才放 —— 同步函式簽名不變，只改三個 async 呼叫端（`append_pdu`、`backfill_pdu`、`set_profile_keys`）。−1 不用鎖。**收集器**先持鎖再讀計數再 `media.collect()`。鎖序：media 鎖在房間 `insert_lock` 之內取放，收集器不拿房間鎖，retention 的鎖與 media 鎖不重疊，沒有環。**審查抓到的（rumia）**：rebuild 刪孤兒時鎖只包住刪除、沒包住決策 —— 決策讀的是開頭重算的快照，而 `collector_paused` 只停收集器、不停正在 +1 的人，所以窗口在 rebuild 這條路上還在。修成持鎖下重讀計數，> 0 就跳過，與收集器同型（`1b426b366`）；設計文件 §3.3 記下「離線視窗內不會觸發，但正確性不該靠大家都遵守視窗」。三份審查 APPROVE。真伺服器 e2e5 四情境重跑全綠、沒有 hang（加鎖後最要看的）。競態本身沒有自動化測試，靠讀碼確認鎖序。 |
| 2026-09-03 | `media/chunked-upload-a`（#16） | ✅ | **分塊上傳的 A 支，也是 fork 第一個使用者看得到的功能。** 提案 [chunked-upload.md](docs/design/chunked-upload.md)（PR #15 同意），pack 外框 [wbf-wire-format.md](docs/design/wbf-wire-format.md)，給 client 開發者的線上規格 [chunked-upload-spec.md](docs/design/chunked-upload-spec.md)。**review 期間模型重定了三次**，每次都是維護者指出我把不該 server 管的事放進 server（§9 逐條有紀錄）：(1) 第一版「先報上限、seal 給真值」的固定幾何，reviewer 抓到 seal 存出超長物件，追下去發現串流根本送不出尾塊，整個拔掉；(2)「第 0 塊定長度、後面每塊一樣長、邊界用乘的」被否決：那是 server 在檢查它不該懂的封裝長度；(3) 把檔名、MIME 明文放 Create 的 meta 是洩露，整個拿掉是躲問題，定案「加密傳」。**最終模型的不變式**：server 知道的只有明文尺寸與收到的紀錄，不判斷任何一塊密文「該」多長；完整性只有 CRC 對、序列沒漏；下載把每一塊照上傳時的樣子交回去。⚠️ **行為與限制**：分塊媒體對舊 client 不相容（標準下載拿得到、解不開；單塊且用 Matrix 標準附件加密才可能相容）；`media_upload_max_len` 預設 10 GiB 且推出塊數上限，超過就截斷並標 `truncated`；進行中的上傳寫 DB（維護者選這條，不要記憶體帳），server 重啟可續傳；`max_pending_media_uploads` 維持上游預設 5，分塊與舊式 pending 共用；每塊一列位置，若 B 支 benchmark 說每塊固定開銷太高，下一刀是 seal 時壓表加 LRU，不改線上格式。三位 reviewer（cirno、rumia、salvia）兩輪 REQUEST_CHANGES 後全部 APPROVE；後補：seal 拒絕零塊、發號時的媒體查詢只認 NotFound 為可用。真伺服器 e2e 68 個檢查點（五個情境：基本流程、abort 與 sweeper、變長塊與續傳、截斷、串流）。**留給 B 支**：WebSocket 通道、benchmark、邊上傳邊下載。 |
| 2026-09-06 | `event/room-seq-recent`（#22） | ✅ | **client 聊天模型要的兩件事（issue #20），也是 wbf 通道第一個非媒體的操作。** 設計 [room-seq-and-recent.md](docs/design/room-seq-and-recent.md)（提案 #21）。核心決定：兩個位置**寫進存起來的 PDU 的 `unsigned`** 而不是讀時補 —— issue 列的七條路徑最後全讀 `pduid_pdu`，一個寫入點全部涵蓋，未來新讀路徑也不會漏；代價是兩處要跟著（redact 剝 `unsigned` 後放回、聯邦送出剝掉）。`r_seq` 是本站的**到達順序**（維護者問過聯邦晚到的訊息怎麼排：即時送來的都拿正號接尾巴，負號只給 `/backfill` 的歷史）。實作期維護者改了三次形狀，都對：命名 `seq`→`r_seq`、加 `g_seq`（全域序號，`/messages` token 那個數，client 當水位）；`Event/Recent` 從「always 拿一萬」改成 **client 帶快取的 `cg_seq` 只拿差異**（差距超過 limit 回最新的 limit 則、`complete=false`、帶 `before=next` 補洞；`cg_seq=0` 等於沒設）；meta 不用 `count` 這個字。**review**（cirno、rumia APPROVE；salvia COMMENT）抓到的：`get_seq_bounds` 讀到壞列退回零會讓 room 重編號 → 改 fail closed 回 `Err`；byte 截斷的游標分支沒有會紅的測試 → e2e 加 `wbf_data_max_bytes=1500`／`200` 兩個情境。⚠️ **行為與限制**：`g_seq` 對讀者不連號（別的 room 也在消耗），只能當水位不能相減算數量；房間內數量用 `r_seq`。一則事件自己大於 `wbf_data_max_bytes` 時 `Recent` 跨過它並 `debug_warn`。既有庫啟動時 `backfill_room_seq` 一次改寫全部 `pduid_pdu`（只讀 key 分組、逐筆點讀寫回），只跑一次。順手：`[profile.e2e]`（release 去 LTO，改一檔重建 9 分鐘 vs 20 分鐘以上）。真伺服器 e2e8 **37 個檢查點全綠**（含 PR #18 版 binary 建的庫換新 binary 啟動被編號、再啟動不重編）。**留給下一支**：E2EE 下引用計數的破口（[media-attachments.md](docs/design/media-attachments.md)）。 |
| 2026-09-04 | `media/chunked-upload-b`（#18） | ✅ | **分塊上傳的 B 支：WebSocket 通道，加上 review 中長出來的中間層與規格向量。** 通道本身很小（`ws.rs`）：升級時驗 Bearer、每個 binary message 一個 pack、依到達順序處理依同一順序回、同一個 `handle_pack`、`Hello` 回 server 資訊、字串 frame 回 Corrupt、超大 frame 由 socket 層拒、`wbf_ws_idle_timeout`（新 config，預設 300 秒）沉默就關。**第一版放了一張每連線的 `id → 下一塊` 表想省一次 DB 讀，rumia 與 salvia 抓到它不是真相卻先於真相否決**：冪等重送把指針倒退、跨傳輸時過期，合法的塊被誤拒 —— 整張拿掉。維護者接著定了正確的中間層：`mediaid_upload` 只剩宣告（Create 寫一次），新 CF `mediaid_upload_progress` 幾十 byte 每塊重寫、與塊位置同一個 txn；service 記憶體 `uploads_hot` **只放進度與驗塊要用的數字**（不放加密描述，seal 時才讀一次），txn 執行後才推進，DB 寫失敗在這個引擎是 fatal 所以記憶體永遠不跑在列前面；`upload_locks` 每上傳一把鎖，順便關掉原本就在的「同 id 兩塊同時到都寫同一 offset」競態。每塊成本：一次檔案寫、一次小 txn、零 DB 讀。**client 共用協議的方式定案**：規格＋黃金向量，不共用程式碼、不用 submodule；`docs/design/wbf-vectors.json` 由 `src/core/wbf/vectors.rs` 產生（15 個 pack、9 個壞 pack、`EncryptedFileInfo`、CRC），server 測試對著它跑，client 複製一份對著測。⚠️ **行為**：WebSocket「只接受 TLS」由前置代理保證，server 不檢查；idle 關線不影響上傳進度，重連從 `Status` 接。三位 reviewer 兩輪後 APPROVE。真伺服器 e2e6（HTTP，66）與 e2e7（WS，34）全綠，含重送、HTTP／WS 交錯、idle 關線。**留給之後**：server 主動推送（邊上傳邊下載）、串流訊息、發送拆成 mpsc task、benchmark。 |
| 2026-09-06 | `media/attachments`（#24） | ✅ | **E2EE 的破口逼出兩件事：引用的來源換成宣告，引用的形狀換成持有者集合。** 起因（[media-attachments.md](docs/design/media-attachments.md) §1）：計數的 +1 來自 server 讀 content，`m.room.encrypted` 的附件 URL 全在密文裡，所以 E2EE 附件永遠計不到（漏水），而 `migrate-references` 重算會把它們當孤兒**刪掉**（活附件）。維護者定方向：既存媒體不管、不計、不刪；計數 0 的由後台掃描清（保護期至少 7 天）；指針由送訊息的請求夾帶、不進事件（事件乾淨、不進雜湊與聯邦）；`migrate-references` 整個拔掉。**實作到一半維護者再推翻計數器本身**（[media-holders.md](docs/design/media-holders.md) §1）：三個 PR 每次都在補「誰負責這個 −1」，第三次 review 仍抓到雙扣（purge 先照列扣、`purge_original` 退回讀備份 content 再扣），問題在**數字不記得是誰扣的**。改成外鍵集合：`(room_b, 1234)` 拿掉兩次第二次是 no-op，冪等由資料結構保證，不靠小心。外鍵用 `g_seq` 不用 `event_id`：purge 的邊界就是一個位置，前綴／範圍掃反向索引不必解析事件；刪房是 room 前綴。加速索引的原則：平時只多寫一筆永不讀、清除時當指標，要多快就多加幾張。⚠️ **行為與限制**：(1) **client 在 E2EE 房間送附件必須宣告**（spec §12），否則上傳 7 天後被掃掉；Element 之類的舊 client 會收到 server user 一則 DM 警告（每人一次）。(2) **既存媒體永不自動刪**（沒有 `mxc_managed` 列），也不再有重算工具；`!admin media refcount` 改印持有者清單。(3) `mxc_refcount`、counter merge operator 的使用、哨兵、`eventid_mxcs` 全部退場（CF 標 `DROPPED`）；`media_gc_migrate_skip_recent_seconds` 變 unknown key。(4) 同 `txn_id` 重送先於宣告驗證回原事件。(5) `WBFUWUNEL_MEDIA_GRACE_SECONDS` 環境變數不夾 7 天底線，**只給測試**。review（rumia、cirno、salvia）抓到的：`check_attachments` 與 append 之間的極窄窗口（接受，記在 media-attachments.md §6.1）、`room_mxc` 無界成長 → 加 `mxc_room` 反向在媒體刪除時清、admin 強刪被持有的媒體留下幽靈持有者 → `media.collect` 統一 `forget_media`。真伺服器 e2e9 26、e2e10 13、e2e8 37 回歸全綠；e2e 腳本從這支起進 `tests/e2e/`。**留給下一支**：合併後的再審與外部審查的驗證結果在 [review-followups-2026-09-06.md](docs/design/review-followups-2026-09-06.md)。 |
| 2026-09-06 | `media/managed-origin`（#26） | ✅ | **PR #24 合併後再審抓到的 P1，加三條小的。** 起因（[review-followups-2026-09-06.md](docs/design/review-followups-2026-09-06.md) §2.1）：`create_file_metadata` 對任何一列都「`mxc_managed` 沒列就寫現在時間」，而「沒列」分不出「新媒體」跟「模型上線前的既存媒體」。一張舊圖第一次被人看縮圖，server 生成縮圖寫列，順手替它補了 `mxc_managed`；舊事件從沒建過持有者，7 天後掃描把整個 mxc 刪掉，活訊息指向 410。這是外部審查第 2 條（舊媒體生成縮圖後失去哨兵）在新模型上的同一形狀。維護者的判斷是**只有 `create` 與 Seal 確立受管**，縮圖不需要自己的 mxc 或外鍵（同一個 mxc、前綴刪除已經讓壽命跟著原檔）；實作成呼叫者必填的 `FileOrigin`，不在 consumer 端靠查表推。**紅燈驗過**：把判斷暫時改回舊行為重建，e2e10 情境 4 讀到 `held=410 free=410 thumb=410`，連被訊息引用的既存圖都被掃掉；還原後 17/17。同支：頭像併發的幽靈持有者（§2.7，`set_avatar_ref` 改讀索引，冪等自癒）、`(mxc, Interfix)` 前綴（§2.6；review 中 rumia 再抓到 `release_room` 的 `(room,)` 同形，一起補）、墓碑 TTL 文件講反話（§2.9：Universal compaction 的 `ttl` 不刪 key，只有 FIFO 會；不改 FIFO 因為它會連新墓碑一起丟）。⚠️ **行為**：既存媒體（無 `mxc_managed` 列）不論被看多少次縮圖都永不自動刪；墓碑永久保留，一筆約 80 byte。三份 review（cirno、rumia、salvia）APPROVE。e2e9 26、e2e8 37 回歸；六個 lib crate 單元 960 綠。📎 順帶記下的坑：PowerShell 5.1 把 `"$id?width"` 讀成變數 `id?`（`tests/e2e/README.md`）；`cargo test --workspace` 在 Windows 會拉進主 crate 的 jemalloc 而死，要逐 crate 跑。**留給下兩支**：§2.3／2.4（`wbf/auth-and-ws-lifetime`）、§2.2／2.5／2.8（`media/upload-lifecycle`）。 |
| 2026-09-07 | `wbf/auth-and-ws-lifetime`（#28） | ✅ | **外部審查兩條 P1 的修法（[review-followups-2026-09-06.md](docs/design/review-followups-2026-09-06.md) §2.3／§2.4）。** (1) `/_wbf/v1/pack` 與 WS 升級的 `authenticate` 只查 token 與到期，沒走標準路由的 `locked_check`：被鎖的帳號拿保留下來的 token（MSC3939 刻意不撤）照樣上傳下載。現在同一套。(2) WS 升級驗一次就永久：登出、撤 device、到期、鎖帳號都關不掉一條開著的連線；而且 `State` 是 `Services` 的裸指標，升級後的 socket task 活得比 request 久，正常關機後 `Arc::try_unwrap` 不知道它還在，是 use-after-free 的路徑。修法：`Session` 每個 message 重驗（一次點讀加一次鎖定讀，跟一個 HTTP 請求一樣；🚫 不做時間快取）；`Services.connections` 追蹤 task、`stop` 先 join 完再拆別的。**review 抓到的**（cirno、rumia APPROVE；salvia LGTM）：join 無超時會被一條卡死的連線卡住整個關機 → 加 15 秒後 abort；decode 失敗分支跳過重驗 → 重驗移到解碼之前；`is_running` 排在 token 查詢之後 → 對調；`until_shutdown` 每圈重訂閱 → 一次。⚠️ **行為**：關機時 client 收到 Close **1001**（第一版送 IANA 的 1012，.NET 的 `ClientWebSocket` 把它當協定錯誤 Aborted，改 RFC 6455 本文的 1001 後正常收到 Close；這是 e2e 抓到的，不是讀碼）。e2e7 情境 3：鎖帳號兩個傳輸 401、登出後下一個 Ping 被拒並 Close 1008、連線開著時 admin room `!admin server shutdown` → Close 1001、程序乾淨退出、log `open=2 → ended`。📎 `release_max_log_level` 把 `debug!` 編譯掉，e2e 探針只能用 info；「沒有懸空」是靠 join 完加乾淨退出間接證。六個 lib crate 單元 960 綠、e2e6 回歸。**留給下一步**：WS 的 `Login`／`Refresh`／`Logout` subtype 提案（建在同一個 `Session` 上）。 |
| 2026-09-08 | `wbf/pack-pipeline`（#33） | ✅ | **pack 管線第一部分（[wbf-pack-pipeline.md](docs/design/wbf-pack-pipeline.md) §1–§6，提案 #32）。** 維護者 2026-09-07 的 checklist：協議講了封包與架構，沒講「設計」——一個 pack 從連線進到回應出的整條線，上傳、登入與以後每個 HTTP→WS 都走它。維護者定的：client 多連線分工 server 不管；每 device 4 條、踢新的、匿名與 HTTP 不算；`Recent` 不能一包一萬條，改成 client 拉的視窗（預設 320、上限從 10000 壓到 500）配 `Batch` 串流，`{tc, bc, fs, ls, r}`、`r = 0` 結束、不做結束 pack；HTTP 之後是 debug／fallback。**三個跟提案不同的地方**（文件標 📎）：(1) 名額在 users service **寫 token 之前**拿——Matrix 語意下同 device 再登入會換掉 access token，先發再拒等於被拒的登入踢掉了舊連線，所以 `issue_session`／`refresh_session` 多一個 `admit` 閘門（HTTP 傳 `admit_any`），e2e7 [5.3b]/[5.3c] 驗；(2) `Recent` 一趟收齊一窗再切，不是兩趟先數再送——一窗 ≤ 500 則 RAM 可忽略，少一個「數的規則與送的規則」會漂的接縫；(3) 一問一答的 handler 保留原簽名由派發者送，只有 `Recent` 與 `Session/*` 拿 `reply`。**review**（cirno、salvia、rumia 三個 APPROVE）rumia 兩條 🟡：Close 入隊無限等 → `enqueue_close` 最多等 5 秒、最壞記憶體界（32 × 16 MiB 一條）寫進 config；first byte 沒量 → e2e8 情境 4，30 房間一窗 320 冷 25 ms、暖 106 ms，數字在 §6.3。⚠️ **行為改變**：`Recent` 在 WS 上的回應由 `Ack` 變 `Batch` 串流、走 HTTP 由回一頁變 `Unsupported`；`Session/*` 走 HTTP 的錯碼 `Conflict` → `Unsupported`；`wbf_recent_max_limit` 預設 10000 → 500；WS 升級與通道登入受每 device 上限。沒上線過，不背相容（維護者）。client 端要跟的東西開在 wbf-matrix-client #15。e2e7 全過（新情境 5）、e2e8 36/36、e2e6／9／10 回歸、單元 971 綠。📎 e2e 教訓：`$b` 蓋掉 helpers 的 `$B`，server 重啟看起來像沒起來；接收要有時限。**留給之後**：第二部分 review-followups §2.5／2.2／2.8。 |
| 2026-09-07 | `wbf/session-login`（#30） | ✅ | **登入走通道（[wbf-wire-format.md §6.3](docs/design/wbf-wire-format.md)，提案 #29）。** 維護者提的方向：登入該有 WS 專用的 pack；定了四點——Logout 直接關線、未登入 30 秒就斷（從升級起算、Ping 不延長）、同一連線重複 Login 允許（就是換帳號）、限速預設開。**兩個架構決定**：(1) kind 用 §3.3 早留的 `0x10 Session`，不塞進 `Control`（一章一個 kind）；(2) 登入本體抽到 service 層，HTTP 與通道叫同一份，`Login` 的 meta 用 ruma 的 `/login` 型別解——兩邊不會漂。**限速是必做不是加分**：HTTP `/login` 之前完全沒有限速，開通道等於給暴力破解開快車道；`IpTokenBuckets` 從 OIDC 那組抽出來共用，四個入口一個 bucket。**review**（cirno、salvia APPROVE；rumia COMMENT）三條 🟡 全修：Refresh 失敗的碼丟了 `soft_logout` → `refuse_login` 先看 `ErrorKind::UnknownToken` 回 `Unauthorized` 帶旗標；`refresh_session` 沒查鎖定（舊 `/refresh` 一直有的洞）→ 補 `locked_check`；限速 key 是 client IP、沒設 `ip_source` 時 XFF 可偽造 → 寫成部署須知。⚠️ **行為改變**：`/login` 與 `/refresh` **預設開始有限速**（每 IP 每秒 1、突發 10，`login_rc_per_second = 0` 關）；WS 升級不再要求 Bearer；**限速先於憑證檢查**，bucket 空時鎖定帳號也回 `RateLimited`（刻意）。📎 **既有行為未動**：refresh 不撤舊 access token。e2e7 情境 4 八組全綠（含匿名連線一直 Ping 也在 3.1 秒被關、通道打滿 bucket 後 HTTP `/login` 也 429、鎖定帳號 Login 與 Refresh 都拒）；e2e6 回歸；六個 lib crate 單元 960 綠。📎 e2e 教訓：第一次跑 [4.1c] 顯示 8 秒沒關，追下去是腳本的 receive 在關線時拋例外沒接住，server 3.1 秒就關了——紅燈也要看清楚為什麼。**留給之後**：`media/upload-lifecycle`（review-followups §2.2／2.5／2.8）等維護者的 checklist。 |
| 2026-09-10 | `wbf/channels`（#36） | ✅ | **WS 訂閱與推送（[wbf-event-push.md](docs/design/wbf-event-push.md)，提案 #35）。** 維護者 2026-09-08 定的順序：先做重點功能，`media/upload-lifecycle` 晚點；這是三件工作裡的第二件（第三件是 Draft Message）。**訂閱者是連線不是使用者**（維護者定）：一個人的每個裝置都要收，而哪條連線處理是 client 的事；`connection_id` 升級時發、`Hello` 回報、只在 process 內有意義。channel 是**資料庫成員資格的記憶體投影**，接點只有一組（`state_cache` 的 `mark_as_joined`／`mark_as_left` → `follow`／`evict`），重啟清空，安全方向是「少推」，`Recent` 補得回來。**推送絕不阻塞 append**：`try_send`，佇列滿就丟並記 `gap`，下一個推得進去的 `Push` 帶 `gap: true`。**review 三輪，抓到的都是真的**：(1) 🔴 `push_window` 只按則數切包、沒按 `wbf_data_max_bytes` 切 —— 一窗 10 則大事件可以湊出超過連線 `max_message_size` 的一包，catch-up 直接被 client 拒收；修法不是抄一份條件，而是把切法抽成 `core::wbf::events::list_pack_ranges`，`Event/Batch` 與 `Event/Push` **共用同一份**（文件承諾一個規則、程式有兩份實作，本來就會漂）。(2) 🔴 `Subscribe` 與 kick 的競態：成員檢查通過之後、登記之前落地的 leave，它的 `evict` 在 registry 查無此人是 no-op **而且不會再來**，那條連線會一直收那個房的事件到斷線 —— 修法是登記後再讀一次成員資格，剔掉的房列進 Ack 的 `skipped`。(3) 連線上的 `Login` 換帳號時舊身分留在 `by_user`、超過 `wbf_data_max_bytes` 的事件推了但 `Recent` 永遠不給、`listeners` 快照到 `push` 之間的 `evict` 沒算 —— 三條都在「誰讀得到什麼」那條線上，都改成在用到答案的地方自己再問一次。⚠️ **client 契約**（`wbf-event-push.md` §2.1，三條都是踩過才寫下來的）：水位認 `ls`／`fs` **不認 `seq`**（丟掉的包也佔號）；**沒收到 `gap` 不等於沒漏過**（gap 掛在下一個推得進去的 `Push` 上，房間安靜下來就永遠不到）；**點名訂閱不要拿非訂閱房的補窗事件推進水位**（`cg_seq` 那輪的補窗是全域的，server 端依 `rooms` 過濾是另一個提案，還沒做）。單元 985 綠（新增 channels 8 條、events 5 條）、e2e11 19/19（含背壓：停讀時 100 則 60 KB 不擋 append、恢復後 `gap: true`、`Recent` 補齊）、e2e8／e2e9 回歸。📎 e2e 教訓：超時的 `ReceiveAsync` 不能丟（下一個 frame 會被它吃掉）。**留給下一支**：Draft Message（`streaming-messages.md` §3–§8）。 |
| 2026-09-11 | `wbf/error-codes`（#38） | ✅ | **錯誤詞表落地（[wbf-wire-format.md](docs/design/wbf-wire-format.md) §2.1／§3.4，提案 #37）。** 起因是 `parse_meta` 把「你的 JSON 壞了」報成 `Conflict` —— 追下去發現同一個字被十一個呼叫點各自解釋，因為**碼是字串、每個呼叫點自己編**。維護者定的三條：每個碼有**序號**（`code_id`）與名字、**一對一、都不重用**；**碼一律事先定義**（先在 §3.4 加一列，才能寫程式）；client **不認得就不認得** —— 當成「失敗了，而且不知道能不能重試」，不重試、往上報，🚫 不要照序號範圍自己補一套行為。實作上 `RejectCode` 就是那張表的閘門：**加不了變體就編不出新碼**，序號從 1000 起（所以反序列化拿到的預設 `0` 永遠是「不認得」而不是某個真碼），百位說明家族。同支把「兩個壞包就關線」換成**連線健康計數器**：`wbf_ws_corrupt_budget`（8）數**連續**解不開的框，中間只要有一個解得開就歸零。**review 抓到的（cirno）**：`decode` 在驗 CRC **之前**先讀 kind 位，所以「框完全正確、只是 kind 未分配」也是 decode 的錯誤 —— 第一版把它算進預算，等於一個守規矩的 client 送八個 `UnknownKind` 就被關線，**違反這支自己寫的規則**；改成只有 `is_undecodable_frame()` 的碼扣預算。⚠️ **行為改變**：格式錯的請求從 `Conflict` 改回 `InvalidRequest`；每個 `Error` 的 meta 多 `code_id`。e2e11 情境 3 釘住兩邊（九個 `UnknownKind` 連線不關、八個解不開的框關線 1002）。 |
| 2026-09-12 | `wbf/streams-core`（#42） | ✅ | **純重構：WS 訂閱與推送抽成共用核心，記帳單位從「每條連線一份」改成「每段會話一份」。** 起因是 to-device 要第二種訂閱，而 `service/channels` 的每個部件都寫死了房間。維護者定的形狀：**一條連線背多種訂閱是正常形狀**（不是一種訂閱開一條連線），所以 `Subscribers<Topic>` 只管「誰在聽什麼、每段會話的 `id`／`seq`／`gap`、推送永不阻塞」，**pack 是呼叫者的**（`push_with` 的閉包），政策（誰能進、什麼時候進出、meta 長什麼樣）留在各自的 kind。順帶把「跟進之後加入的房」從訂閱者身上的旗標改成**一個 topic**：join hook 因此問的是跟其他人同一張索引，而不是第二張會漂的表。**review 三輪抓到兩條真的**：(1) 🔴 我把 `follow`／`evict` 從「一把寫鎖內查＋改」重構成 `listeners()`（讀鎖）→ `enter_topic()`（寫鎖），中間關掉的連線會被插回 topic 而**再也沒有東西會來清它** —— 那個房從此永遠 `is_listened`，每則事件都為不存在的人序列化一次。修法是收回**單一交易**（`copy_topic`／`leave_topic_of_user`）並**把 `enter_topic` 整個刪掉**，讓分兩把鎖的版本寫不出來。(2) 🔴 Ack 的 `joined` 用 `entered.saturating_sub(1)` 推算房數，帳號層重訂閱時 `FollowsJoins` 已在索引裡、只有新房是新的 → 一個真的新房被報成 0；修法是核心回傳**哪些** topic 是新進的，房間層自己數 `Room(_)`。⚠️ 兩次我都在 PR 內文寫過「行為一個字都沒改」而那是錯的，兩次都記在 commit 與回覆裡。同支把三份現行設計文件裡指向已刪模組（`Services.channels`、`src/service/channels/mod.rs`、`channels::relay`）的字改掉 —— 它們不是 changelog，是下一階段會照著實作的東西。單元 996 綠、e2e11 22/22。 |
| 2026-09-12 | `wbf/to-device`（#43） | ✅ | **to-device 佇列走通道（`0x16 Device`，[wbf-to-device.md](docs/design/wbf-to-device.md)，提案 #41）。** 起點是 client repo 的需求（issue #39）。`Subscribe`／`Unsubscribe`／`Push` 照房間那套、`Fetch`／`Batch` 照 `Recent`，**差別只有一個**：client 會把拿到的東西**銷毀**，因為那個佇列是 Megolm 金鑰的唯一副本。所以銷毀是**帶結果的命令**不是回執：`ItemsDestroy` 逐則指名（不是前綴 —— client durable 存下來的未必是收到那串的前綴）、`tc` 與 data 對不上就**一則都不刪**、「沒了」是**讀回來確認**的（這個引擎的 `del` 沒有東西可回）、已經不在的算銷毀成功（client 要的是狀態不是事件）、一則都沒刪也回空清單（否則跟「server 沒回應」分不出來）。**維護者 2026-09-12 推翻了提案原本的綁定規則**：不是「第二條連線回 `Conflict`、先來的不動」，而是**後來的接手** —— 代價不對稱，搶佔最壞是重推一次還在佇列裡的東西，拒絕最壞是**半開連線讓這個 device id 永久廢掉**（到 idle timeout 之前根本訂不進來）。被接手的那條收到新碼 `Superseded`(1505，照 §3.4 先進表再發)，帶 `IS_LAST`、`id` 是**它自己當初 `Subscribe` 用的 id** —— 靜默失效是這裡最糟的結果。⭐ 這條規則**屬於註冊表不屬於 handler**：建構串流時宣告 `Occupancy::OneTheLatest`，進 topic 的同一把寫鎖內執行；第一版是呼叫點「先查再登記」的兩把鎖，**兩條同時來的連線會雙雙進去**，獨佔性根本不成立（三位審查者各自獨立定位）。**review 另外兩條 🔴**：(1) 同一條連線 `Login` 換身分時叫的是 `unsubscribe_all`，而那個名字只清房間 —— 舊身分的 to-device 項目（**別人的金鑰**）會被推進現在屬於另一個人的佇列，而新 session 不是持有者、銷毀會被拒，那個裝置就此卡死。⭐ **名字本身就是 bug**：改叫 `unsubscribe_all_rooms`，換身分改叫本來就存在的全清入口 `remove_connection`（🚫 沒有照建議讓 `unsubscribe_all` 清兩條串流 —— 那會讓 client 退掉房間訂閱時連金鑰一起停掉）；**紅燈驗過**（把舊呼叫改回去重建，e2e12 報 `leaked=1`）。(2) §7 承諾的啟動斷言沒落地 → `check_wbf_device_window`，一窗包數放不進發送佇列就拒絕啟動；它**第一次跑就抓到一份真的矛盾設定**（e2e11 為測背壓把佇列縮到 4 卻留著 10 包的窗）。📎 後果的描述也一起改對：`Fetch` 的 `Batch` 是 `await` 送的，放不下不會掉包，會是 handler 卡在半窗佔著名額。⚠️ **行為**：`Device/Fetch` 的 `limit: 0` 回空的 `Batch`（不夾成 1）；`remove_device` 既有的前綴刪除是無窮 TTL 唯一的自動回收路徑；新增 `!admin user to-device-queue <user>`，**只印 count 不印時間**（`next_count` 是純遞增計數器、不帶時鐘，印時間就是說謊）。單元 1018 綠、e2e12 19/19、e2e11 22/22。📎 e2e 教訓：整晚看起來像 hang 的批次，根因是 e2e11 在腳本層用 `$p` 蓋掉 server 的 process handle，`Stop-Server` 於是拿封包當 process、砍在 PID 0，server 一次都沒被殺到。**留給下一支**：Draft Message（工作 3），以及 client 端的協議同步。 |
| 2026-09-12 | `wbf/draft-message`（#45） | ✅ | **草稿訊息走通道（`0x02 Stream`，[streaming-messages.md](docs/design/streaming-messages.md) 第四版）。** 三件工作的最後一件。草稿是一則**還在長**的訊息（LLM 逐字吐、長訊息分段打），而設計的重點是**看著它變不留永久痕跡**：`Draft` 寫一則明文錨（它的 `g_seq` 就是草稿 id），之後的 `Keypoint`／`Delta`／`Append` 只廣播、不進庫，歷史上只留「被 redact 的錨 ＋ 完成的訊息」兩筆。⭐ **server 對草稿零狀態**：誰能寫、還開著沒有，每一片都從錨重讀（一次點讀，**刻意不快取** —— 快取就是 server 端的草稿狀態，第三版正是為此作廢）；server 保證的是**這片是誰寫的**，所以接收端不必再驗。**維護者在審查期間加了一條協議改動**：片的 `data` 前 4 byte 是 `prev`（它接在哪一片之後），因為 `Delta`／`Append` 不知道自己套在什麼之上 —— 中途加入與漏收一片的人**都無從判斷自己同不同步**，而原本唯一的線索是「`seq` 應該剛好大 1」這個算術假設。片號因此**從 1 起算**，`0` 只留給「沒有基底」。⚠️ **外部審查抓到七條，全部成立**，而它們是同一個形狀：**一則開著的草稿是一張不會過期的許可證** —— 我把「開稿時檢查過」當成「之後都還有效」。修法是兩條原則：**每片重問「現在還能不能發」**（停權 `is_suspended` ＋ 房間 power level；⭐ `Abandon` 故意不擋，撤回自己的東西是停權者仍有的權利），以及**發許可證的門只能有一道**（`org.wbftw.wbfuwunel.draft` 成為**保留事件型別**，一般 `/send` 與 `Event/Send` 一律拒絕，否則人數上限只綁得住自願走 `Draft` 的 client）。另外兩條是可見性：草稿繞過了 `user_is_ignored`（現在跟一般房間推送同一套收件人政策，而因為選收件人要 await DB，送出走新的 `relay_to` **在鎖內重驗成員資格**），以及成員檢查晚於讀錨讓非成員能用 `NotFound`／`Conflict`／`Forbidden` 的差異**探測私人房間的事件狀態**（成員檢查移到讀錨之前，免費）。最純的一個 bug 是 `R1`：txn id 用 `{連線號}-{seq}` 組，而**連線號每次開機從 1 開始、txn 卻存在 DB** → 重啟後第一條連線的第一個 `Draft` 拿回上一輪的錨（可能已 abandoned、可能在別的房間）；修法是 `Streams` 帶一個開機隨機 epoch。⚠️ **`Stream` 的 flags 兩個方向都是 0**（維護者定）：四個旗標都在講「這個包跟收件人的關係」，轉發之後沒有一個為真 —— 連 `META_ENCRYPTED` 也不行，因為這個 kind 的 meta 就是 server 必須讀的 room id；**擋在入口**而不是出口遮罩，所以「原樣送」字面上仍成立。順帶：redact 的政策收成一份（`redact_event_as`，HTTP 路由與 `Abandon` 共用），`IpTokenBuckets` 泛化成 `TokenBuckets<Key>`（草稿節流以 **device** 為 key，但補充算術只有一份）。單元 1021 綠、e2e11 57/57、e2e9／10／12 回歸。📎 e2e 教訓兩則：`$script:Http` 改名漏改兩支腳本（**改完要掃舊名字**，我有規則卻沒照做，而這支只跑 e2e11／12 所以一路綠到審查）；三條新檢查第一版**綠得很虛** —— 它們的草稿開在已達人數上限的房間、根本沒開成，所以現在多了一條「先斷言這些檢查要用的草稿真的開著」。**留給下一支**：`id` 的型別 byte（#46 提案）。 |
| 2026-09-13 | `wbf/id-type`（#47） | ✅ | **[BREAKING] pack 標頭 `id` 的第一個 byte 是型別（[wbf-wire-format.md](docs/design/wbf-wire-format.md) §2.2，提案 #46）。** 一個欄位一直裝著三種來源完全不同的東西 —— client 自己挑的會話號、server 隨機鑄的上傳 id、資料庫給的事件位置 `g_seq` —— 而**要先看 `kind` 才分得出來是哪一種**。後果有兩層：client 用一張表管會話時，兩件不相干的東西會落進同一格；server 收到對不上的 id 只能回「找不到」，說不出「你把上傳 id 填到訂閱那一格了」。現在 `id = [型別 1 byte] ‖ [值 7 byte 大端]`，`0x00` 無（**整個 id 必須是 0**）、`0x01` client 的會話號、`0x02` 事件位置、`0x03` 上傳 id，其餘未分配（**先進表才能用**）。⭐ **期望的型別放進既有的准入表多一欄，不開第二張表** —— 兩張以同一個 key 索引的表遲早漂；配一條測試釘住表的完整性，因為最危險的不是填錯而是**新增一列時忘了想它**。**不認得就拒絕**：型別不符、表沒定義的 byte（含 `0xFF` 延伸）、「無會話卻帶值」都是 `InvalidRequest`。⚠️ **升級的代價**：進行中的分塊上傳失效（舊 id 是整 64 bit，表達不出來；client 重新 `Create` 即可）、舊 client 每個帶 id 的包都被拒 —— 這就是 [BREAKING]。media id 因此變成 **14 個十六進位字元**（上傳 id 拔掉型別 byte），既有的 16 字元媒體不受影響，🚫 **任何人都不要驗 media id 的長度**。📎 提案裡兩個決定當場回本：「鑄了 id 的 `Ack` 回組好的 id」讓 203 個建包點只有 23 個要改（全在各腳本自己的 helper，因為 client 只在**自己挑號**時才需要知道型別存在），「值維持整數個 byte」讓 media id 直接印成 14 字元、不必補位。一個 e2e FAIL 是規則生效而不是瑕疵：`Session/Login` 原本把請求號塞在 `id`，而 Session 沒有會話 → 以前「`id` 當請求號」與「`id` 是會話」混在同一個欄位沒人會發現，現在當場被講出來。**review 三條 blocking 都成立，而且第一條與第三條是同一個病**：我用 grep 挑著改而不是逐筆列舉，所以修好的剛好是我搜到的。修法改成**不變量測試**（每筆向量的 id 都要解得出型別、`None` 型別必須整個是 0），它立刻抓到兩筆沒人列出來的 —— 第八筆裸值，以及 `ack_draft_open` 的 `id` 是**手寫常數而且寫錯**（`g_seq` 是 4711、`id` 卻是 `0x02 ‖ 4135`），照那個 Ack 實作的 client 會去指一則沒開的草稿。⭐ 而 PR 內文宣稱過「向量的 id 是算出來的、不是手寫常數」—— 那句話當時剛好有一個例外，例外就是錯的那筆。另外 `IdType::None.compose(123)` 原本回 `Ok(123)`，能建出 dispatcher 必拒的 id：錯誤型別擴充成 `IdValueRefused::{TooLargeForSevenBytes, NoneTakesNoValue}` 並 fail closed，round-trip 測試排除 `None`（「原樣讀回」對一個不帶值的型別不是一個性質）。第三條是**現行契約文件全樹掃描**：`streaming-messages.md`（連狀態列都還寫著「草案，等維護者同意」）、`wbf-wire-format.md` §3.2、`chunked-upload.md`、to-device／push／recent 三份的「id 由 client 選」、管線表少了這道閘，外加一個順帶抓到的過期契約 —— §3.1 還寫 `Stream` 的 `META_ENCRYPTED = 1`，而草稿定案時 meta 變成明文 room id、帶任何 flag 都會被拒。單元 1032 綠、e2e11 59/59（新增「自己先組 draft id」與「拿裸 `g_seq` 當片的 id」兩條）、其餘 e2e 回歸。 |
| 2026-09-13 | `wbf/upload-lifecycle`（#48） | ✅ | **Seal 只是名義上的串流，外加兩個會刪掉已封存媒體的交錯（[review-followups-2026-09-06.md](docs/design/review-followups-2026-09-06.md) §2.2／§2.5）。** 三件 channel 工作做完後補的上傳生命週期。§2.2 是**同一行往兩個相反方向壞掉**：`store_staging_file` 把暫存檔切成 1 MiB 餵給 `Provider::put`，**還附上真實大小** —— 本地 provider 的 multipart 門檻是 `usize::MAX`，所以「按大小送」永遠選單段路徑、**把整個檔案收進一個 buffer**（10 GiB 的上傳是合法的，封存一個就多 10 GiB RSS，而且是在 server 手上已經有那個檔案的那一刻）；而送 S3 時 **stream 的一個 item 就是一個 part**，1 MiB 的非末段 part 違反 S3 的 5 MiB 下限，`complete()` 直接拒 —— **分塊上傳封存到 S3 從第一天起就不可能成功**，而 `multipart_part_size` 這條路上沒有人讀它。修法是一律串流（`size` 傳 `None`）並用 provider 自己的 part 大小；⚠️ 另補一個 review 沒提到的同族錯誤：`read_part` 會**填滿** buffer 才交出去，因為一次 `read` 就算沒到檔尾也允許少讀，而一個 item 就是一個 part，**中途一次短讀就是中途一個短 part**，換條路走到同一個 S3 失敗。§2.5 兩個交錯的結局都是刪掉**已封存、正在被服務**的媒體的列：`upload_status` 看起來是讀，但 `hot_upload` 沒中快取時會寫回快取，所以「快取空的時候進來的 Status」（重啟後）是寫路徑，跟 `Seal` 交錯能把舊快照塞回去、讓後來的 `Abort` 去 `discard_upload` 掉 `mxc_chunk` 與 `mxc_chunked`（含加密描述）；sweeper 則是用鎖外拍的快照判過期，等它拿到鎖時可能已經有 chunk 寫進去並回了 Ack。前者補鎖（並在 `hot_upload` 上寫死「每個呼叫者都必須已經持有那個鎖」），後者在鎖下重讀進度列。🚨 **這一輪最值得記的是差點收下一個假綠燈**：e2e6 情境 6 的第一版只用 `PeakWorkingSet64`，對著**沒修的 code** 報「漲 0 MiB、streamed=True」—— peak 是整個行程生命期的高水位，256 MiB 的尖峰只要沒超過同情境前面 16 個 16 MiB 請求造成的水位就完全看不見。改成「Seal 還在飛的時候取樣」之後：沒修 **256.4 MiB／False**，修了 **10.3 MiB／True**。**review 三位指向同一行，而那行裡有兩個不同的錯**：`.min(8 MiB)` 是無條件的（operator 設 10 MiB 被默默改成 8 MiB —— 正確性沒壞，但一個設定值從此不代表任何事），以及**沒有下限**（設 `1 MiB` 或 `0` 就原樣回到這支要修的 bug）。⭐ 修法不在那個 call site，因為 `multipart_part_size()` **還有第二個呼叫者**（`put_one`，一般大檔媒體那條路），寫在 call site 會讓更舊的路留著同一個洞；下限進 accessor，回傳型別改成 `Option<usize>` —— 用 `usize::MAX` 當「沒指定」的哨兵值**正是「沒有大小」與「有一個該尊重的大小」會被同一個 `.min()` 混掉的原因**。另加啟動檢查 `check_s3_part_size`：低於下限**拒絕啟動**而不是偷偷抬高，因為 S3 會乖乖收下每個過小的 part、最後才拒絕整個上傳，症狀是「每個大檔都在最後一步失敗」而現場沒東西指向設定檔。⚠️ **兩件沒驗也不假裝驗過**：S3 那半沒有 MinIO 可跑（靠 S3 規格與 `object_store` 的行為推斷，但下限現在是程式保證的）；sweeper 的重讀沒有 e2e，因為 sweep 只在啟動與每小時跑，腳本塞不進「掃完清單、還沒拿到鎖」那個縫 —— 硬寫只會得到一個綠燈不代表任何事的測試。📎 §2.8（#16～#18 之間的舊格式上傳列會永久佔住一格配額與一個 staging 檔）**故意沒做**：維護者 2026-09-13 確認這個 fork 從未真實上線，不可能有那種列。單元 1034 綠、e2e6 情境 1–7。 |
| 2026-09-14 | `wbf/recent-rooms`（#51） | ✅ | **`Event/Recent` 可以點名房間 —— 一個房 ＋ `before` 就是那個房的歷史。** 起於維護者問「WS 能不能用 `r_seq` 拿單房歷史，寫 `Event/History` 還是移植 `/messages`」。調查結論是**都不必**：移植 `/messages` 會把不透明 token（正是 `r_seq`／`g_seq` 要取代的）與塞不進 `Batch` 串流的回應形狀一起帶過來，而它真正值錢的（filter、relations、聯邦 backfill）HTTP 已經在做；新開 `Event/History` 則是「`Recent` 限定一個房」換個名字，兩扇門遲早給出不一樣的答案。⭐ 關鍵事實是 **`r_seq` 沒有索引**（`roomid_seqbounds` 只存兩個計數器），所以翻頁用 `g_seq`（`pduid_pdu` 的原生 key）、`r_seq` 留給 client 判斷有沒有洞 —— 不必新建任何表。而且點名一個房是**更快**的路：k 路合併退化成單路掃描，就是 `/messages` 用的那個迭代器。🚨 點名了不在的房**整個請求拒絕**而不是默默留白（一扇窗是一個答案，缺一個房的答案是錯的）。**順便關掉一條已知缺口**：`Subscribe{rooms, cg_seq}` 的補窗原本是全域的，文件要求 client「不要拿非訂閱房的事件推進水位」—— 而那等於要求 client 不要相信 server 剛剛送給它的東西。⚠️ 寫進文件的陷阱：**一窗結束是「本站這份副本沒有更舊的了」，不是「這個房間沒有更舊的了」**（Matrix 房間沒有 home server，`Recent` 不像 `/messages` 會去聯邦 backfill；今天 `allow_federation = false` 所以兩者一致）。**review 抓到一個真 bug**（rumia、salvia 各自獨立）：`rooms` 沒去重，同一個房點兩次就被掃兩次、每則事件重複 —— 補窗那條路的重複是這支自己造成的。修在 `collect_window`（消費端，兩個呼叫者都經過）。**每條新斷言都先對著沒修的 binary 跑過**（沒修：18 則只有 9 則不同、補窗推兩次、`skipped` 列兩次），而紅燈順便更正了我一句話：去重拿掉後 `joined` 仍是 1（它來自註冊表的 set），我在註解裡寫「會變 2」是錯的。📎 e2e 教訓兩則，都是這支自己的執行抓到的 harness 坑：(1) 被拒絕的 WS 讓 `Recv-Or-Null` **100% CPU 空轉、沒有輸出**（PowerShell 燒了 1454 秒、server 0.5 秒）；(2) 腳本中途 `throw` 會留下舊 server，下一輪**整輪對著舊 server 跑**（`connection_id=6`）—— `Start-Server` 現在拒絕。另外量到：有推播排著的連線關掉後，名額要 **5.1 秒**（`DRAIN_TIMEOUT`）才回來。單元 1034 綠、e2e8 42/42、e2e11 61/61。 |
| 2026-09-14 | `wbf/queue-byte-budget`（#50） | ✅ | **一條連線的發送佇列數的是包數、而一個 pack 可以是 16 MiB —— 所以一條連線握得住半 GB。** 起於維護者 2026-09-13 問「4 個 client 啟動後最高的瞬時記憶體高峰是多少」：`wbf_ws_send_queue_len` 是 32 **個 pack**，一個 pack 最大 16.07 MiB → 一條連線 514 MiB、4 裝置 × 4 條 ≈ **9 GiB**，而且不需要攻擊者（要求 32 個大塊、慢慢讀 socket 就到得了）。⭐ **這個界本來就寫在設定註解裡**（`32 x 16 MiB = 512 MiB`）—— 寫下來了，只是沒有人把它再乘上連線數。修法是佇列多一個 byte 預算（維護者定 16 MiB），而且**額度跟著 pack 一起排隊**：`Queued` 帶著 permit，送出任務寫完 drop 掉才還 —— 在取額度的地方還會是一樣的程式碼、完全不同的意思（那樣量到的是「能塞多少」而不是「現在握著多少」）。同一支把 `wbf_data_max_bytes` 降到 2 MiB（維護者定），因為這兩個數字是同一個決定：**16 MiB 的滿包根本放不進 16 MiB 的預算**（啟動檢查會拒），2 MiB 的放得下 7 個。⚠️ 代價是一個檔案的塊數變 8 倍（1 GiB ＝ 512 塊，每塊一個 txn）。結果：一條連線最壞從 578 MiB 降到 ≈ 20 MiB，16 條 ≈ 0.3 GiB；閒置實測 42 MiB。**review 三輪抓到的全是文件**：三處註解用舊的 16 MiB 當論據（`refuse_oversized_payload` 用「16 MiB × 聽眾 × demand ＝ 大半個 GB」說服讀者要關連線，換成 2 MiB 是 60 MiB，**理由本身垮掉**；修法是把可設定的值從註解裡拔掉、只寫乘法）、`wbf-to-device.md` 還用現在式寫舊公式（上一輪我只掃了字面值「16 MiB」，同一個事實換個寫法就漏了）、以及「放得下八個」其實是 7 個（我沒把 meta 上限算進一個滿包，PR 描述那張表「16 MiB → 2 個」實際是 0 個）。單元 1039 綠、七支 e2e 全綠（e2e6 的 256 MiB Seal 改成 128 × 2 MiB、e2e7 的超大 frame 改成剛好越線的 3 MiB）。📎 維護者另外提了**大塊懶加載**（佇列裡放指標、寫的時候才讀檔），留作之後的提案：它要改 `Outgoing` 的語意、要給送出任務發 `Error` 的能力。 |
| 2026-09-14 | `misc/window-bytes-and-roadmap`（#53） | ✅ | **一窗數的是則數，而每則只要求放得進一個 pack —— 500 則 × 2 MiB ≈ 1 GiB，而且整窗的 pack 先全造好再送，等於兩份。** 跟 #50 同一種病（界用「幾個」寫，沒人乘出來），是 #50 之後記憶體清單上最大的一個洞。維護者 2026-09-14 指定「加上 filesize 限制，優先於 limit」。⚠️ **加上限的連帶問題比上限本身重要**：被 bytes 截斷的窗則數少於 `limit`，而那正是文件教 client 的「沒有更舊的了」（`tc < limit`）—— 照舊規則會把水位推過還在的事件，所以 `Batch` 多了 `more`。同一個問題有個更早的答案沒人給：`Subscribe{cg_seq}` 的補窗被 `wbf_recent_max_limit` 截斷時什麼都不說，client 每收到 `Push` 就推水位，會跨過剩下的 —— 現在第一包帶 `gap`。審查：rumia 抓到 `limit: 0` 被當成截斷（空窗回 `more: true`；補窗上限設 0 時會先 `mark_gap`，讓下一個不相干的 live `Push` 帶著假 `gap`），cirno／salvia 同意事實、判定非 regression 不擋；還是修了（一行、四條路徑同一處），並先拿舊 binary 跑新 e2e 確認四條鑑別檢查都紅。同一支補齊 #52 漏掉的狀態標記：roadmap §2.3（還是作廢的第二／三版草案）、§2.6、§4.2，README 索引，`media-gc.md` 從 #7 起就寫錯的「提案，尚未實作」等 —— #52 掃的是「#50／#51 改了哪些事實」，沒把狀態標記本身當事實掃。單元 1051 綠、e2e8 47／0、e2e12 22／0。 |
| 2026-09-14 | `wbf/recent-erasure`（#54） | ✅ | **抹除帳號（MSC4025）之後才加入的人，在 `Event/Recent` 讀得到原文。** 起於維護者問「`Event/Recent` 會不會回 redact／edit」（會，跟 `/messages` 一樣）。查的時候發現：Matrix 每個讀取端點送出事件前都過 `bundle_aggregations`，抹除在那裡執行（存的事件不動、送出時裁剪，所以抹除可逆）；`collect_window` 抄了 `/messages` 的兩道過濾（ignore、可見性），漏了這第三道。修前實測：alice 發言後停用並抹除、carol 之後才加入 —— `/messages` 給 carol `content: {}`，`Event/Recent` 給原文；`Subscribe{cg_seq}` 的補窗是同一個窗，一樣。即時推送不受影響：被推的人在事件送出時就在房裡。修法是呼叫同一個函式，不是抄它的判斷，上游之後在那裡加規則不會再漏。📎 這一列在 #58 合併後補上，#54 合併時漏寫了。 |
| 2026-09-15 | `feat/displayname-leaf`（#58） | ✅ | **預設行為改變：沒自己設 `new_user_displayname_suffix` 的 server，之後新註冊的帳號顯示名稱是「帳號名 🍃」，不再是 💕。** 維護者 2026-09-15 指定換成這個 fork 的標記。起於問「註冊完顯示名稱是否會自動加 emoji」：會，所有建帳號的路（一般註冊、訪客、appservice、SSO／OIDC／LDAP 自動建帳號、管理 API、`create-user`）都走 `full_register`，沒有一條會跳過。📌 只在註冊當下加一次 —— 已存在的帳號、使用者之後自己改的名字都不受影響；要關掉設 `""`。伺服器帳號不走註冊，不會有後綴。SSO 頁面（`sso/*.html`）上的 💕 是裝飾，沒動。 |
| 2026-09-15 | `feat/system-user`（#57） | ✅ | **使用者收到伺服器的訊息，看到的是一個叫「conduit」的成員。** 起於維護者問「有沒有 server 專屬帳號、它能不能自由進房插話」：它不能（每個事件照樣過 `state_res::auth_check`，不在房裡發不出去），但查的時候發現這個帳號**從來沒有顯示名稱**，client 拿 ID 前半段頂著，而 `conduit` 是上游的歷史名字。維護者指定 `@system` 與 `[SYS] <server_name>`；69420 不改；不遷移（fork 從未上線）。🚨 **改名的真正風險是權限提升，不是顯示**：`user_is_admin` 只比 ID，資料庫裡如果已經有真人叫 `system`（例如匯入 Conduit 的資料庫），改名後那個人就是管理員 —— 所以加了記號＋啟動閘門，並且啟動時就建立帳號，讓名字從第一刻起被佔住，六條註冊路徑不必各自加保留規則。⚠️ **行為改變**：開發時留下的舊資料庫，管理員房間裡加入的是 `@conduit`，換新版後找不到管理員房間，重建即可。自我審查後的收尾（97ed1d3c5）：join 事件改從 profile 填、刪掉手寫的 `server_user_join()`（兩份遲早漂移，手寫那份沒有頭像）；拿掉走不到的「不存在就建」守衛；`Start-Server` 看到程序已退出就直接失敗，拒絕啟動的場景不再等 20 秒。e2e14 7/7（情境 2 用**改名前的 binary** 讓人註冊 `system`，新版在那個資料庫上拒絕啟動）、e2e9 26/26、單元 1051 綠。 |
